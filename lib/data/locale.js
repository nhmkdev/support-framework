/*
 Localization data (TODO: completely undefined)
 */

ig.module(
	'data.locale' 
)
.requires(
	'impact.game',
	'impact.font',
	'support.util',
    'data.init'
)
.defines(function(){

// TODO: determine how localization will be handled (easy enough to have separate files with each language but all would be in memory...)
    
});